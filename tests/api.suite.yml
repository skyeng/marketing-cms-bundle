actor: ApiTester
modules:
  enabled:
    - \Skyeng\MarketingCmsBundle\Tests\Helper\Api
    - Symfony:
        app_path: '../src'
        var_path: '../var'
        environment: 'test'
    - Doctrine2:
        depends: Symfony
        cleanup: true
    - REST:
        depends: Symfony
        timeout: 60
    - Asserts:
